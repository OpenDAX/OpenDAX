if MODBUS
  M_MODBUS = modbus
else
  M_MODBUS = 
endif

SUBDIRS = $(M_MODBUS) daxc test

#AUX_DIST = $(ac_aux_dir)/install-sh

#dist-hook:
#	(cd $(distdir) && mkdir $(ac_aux_dir))
#	for file in $(AUX_DIST); do \
#	  cp $$file $(distdir)/$$file; \
#	done
