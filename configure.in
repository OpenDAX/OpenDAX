AC_INIT(dcs/main.c)
AC_CONFIG_AUX_DIR(config)
AM_CONFIG_HEADER(config.h)

VERSION="0.1"
PACKAGE=opendcs

AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

AC_PROG_CC
AC_CHECK_HEADERS([malloc.h])
AC_PROG_RANLIB

#CFLAGS="$CFLAGS -Wall"
CFLAGS="-Wall -g"

AC_SUBST(ac_aux_dir)

AC_OUTPUT(dcs/Makefile lib/Makefile modules/Makefile modules/test/Makefile Makefile)
