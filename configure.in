AC_INIT(dcs/main.c)
AC_CONFIG_AUX_DIR(config)
AM_CONFIG_HEADER(config.h)

VERSION="0.1"
PACKAGE=opendcs

AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

AC_PROG_CC

CFLAGS="$CFLAGS -Wall"

AC_SUBST(ac_aux_dir)

AC_OUTPUT(dcs/Makefile Makefile)
