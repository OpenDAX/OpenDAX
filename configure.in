AC_INIT(dax/opendax.c)
AC_CONFIG_AUX_DIR(config)
AM_CONFIG_HEADER(config.h)

VERSION="0.1"
PACKAGE=opendax

AM_INIT_AUTOMAKE($PACKAGE, $VERSION)

AC_PROG_CC
AC_PROG_RANLIB

CFLAGS="-Wall -g"

AC_SUBST(ac_aux_dir)

AC_OUTPUT(dax/Makefile lib/Makefile modules/Makefile modules/test/Makefile modules/modbus/Makefile Makefile)
